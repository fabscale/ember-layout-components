import Controller from '@ember/controller';

export default class WrapperController extends Controller {
  wrapperCode = `<Layout::Wrapper>
  <div>Wrap this content nicely...</div>
</Layout::Wrapper>`;
}
